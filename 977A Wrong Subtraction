# include <iostream>
using namespace std;
 
int main()  {
    int n, k;
    cin >> n >> k;
    while(k--)  {
        if(n == 0)  break;
        else if( n <= 9) n -= 1;
        else if(n % 10 == 0) n /= 10;
        else    n -= 1;
    }
    cout << n << endl;
 
    return 0;
}
